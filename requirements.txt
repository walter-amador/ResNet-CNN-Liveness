# Cross-platform requirements — compatible with Google Colab, local venv, and conda
# Works on: macOS Apple Silicon/Intel, Linux x86_64, Windows x86_64
#
# DO NOT add Jupyter/IPython packages here — Colab manages its own runtime stack.
# (ipykernel, ipython, jupyter_client, jupyter_core, tornado, pyzmq, traitlets, etc.)
#
# GPU (CUDA) note for local installs on Windows/Linux:
#   Install PyTorch first via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   Then: pip install -r requirements.txt
#   Colab already handles GPU torch automatically.

# --- Deep learning ---
torch>=2.1.0
torchvision>=0.16.0

# --- Computer vision ---
# Use only contrib — it includes everything in the base opencv-python package.
# Having both installed simultaneously causes import conflicts.
opencv-contrib-python>=4.8.0

# --- Face / pose detection ---
mediapipe>=0.10.9

# --- Scientific computing ---
numpy>=1.24.0,<3.0
scipy>=1.11.0
scikit-learn>=1.3.0

# --- Data & visualization ---
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
Pillow>=10.0.0

# --- Utilities ---
tqdm>=4.65.0
absl-py>=1.4.0
sounddevice>=0.4.6
